\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{amsmath,amssymb}
\usepackage{siunitx}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage[round]{natbib}
\usepackage{hyperref}
\usepackage{xcolor}

\geometry{letterpaper, left=1in, right=1in, top=1in, bottom=1in}
\hypersetup{hidelinks=true}

\title{Uniform vertical regridding of radiosonde and dropsonde profiles}
\author{Thomas D. DeWitt}
\date{\today}

\begin{document}
\maketitle

\section{Introduction}

Radiosonde and dropsonde profiles report atmospheric variables at irregular vertical intervals that differ between instruments, processing methods, and field campaigns.  To compare profiles across datasets or to compute structure functions at well-defined separation distances, we first place all profiles on a common vertical grid.  The algorithm described here does so by constructing a uniform grid along the altitude axis and averaging all measurements that fall within each cell. If no measurements fall within a given cell, it is marked as missing.

The simplicity of this approach is deliberate. If interpolation was used, artificial smoothness would be introduced where data are interpolated given that commonly-used interpolation schemes assume a smooth function with $H=1$ \citep[cf.][]{lovejoy2007}.

\subsection{IGRA}

The Integrated Global Radiosonde Archive \citep{durre2006} is a quality-assured compilation of radiosonde observations from more than 1500 globally distributed stations, with records extending from the 1960s to the present.  Observations typically include pressure, temperature, dewpoint depression, geopotential height, wind direction, and wind speed.  IGRA integrates data from disparate sources and applies automated quality assurance that removes measurements failing checks for physical implausiblity, internal inconsistency, climatological outliers, and temporal inconsistency.

Here, the data from 2000-2025 are used as provided. 

\subsection{JOANNE}

The Joint dropsonde Observations of the Atmosphere in tropical North atlaNtic meso-scale Environments (JOANNE) dataset \citep{george2021} contains 1215 dropsondes deployed during the EUREC\textsuperscript{4}A field campaign in January--February 2020.  Dropsondes were released from the German HALO and NOAA WP-3D aircraft over the tropical North Atlantic east of Barbados, with the goal of characterizing the thermodynamic and dynamic environment of trade-wind cumulus clouds.  Sondes were deployed along circular flight patterns (${\sim}\SI{222}{km}$ diameter) to enable estimation of area-averaged mesoscale divergence, vorticity, and vertical velocity.  The dataset provides both individual sounding profiles and a uniformly gridded product at $\SI{10}{m}$ vertical spacing.  \citet{george2021} assign each profile a quality control flag of ``good,'' ``bad,'' or ``ugly'' based on launch detection, profile completeness ($\geq$80\% for all variables), and plausibility of near-surface values.  A multiplicative correction factor of 1.06 is applied to relative humidity from HALO sondes to compensate for a systematic dry bias caused by insufficient reconditioning of the humidity sensor before flight.

Here, only profiles flagged as ``good'' are used.

\subsection{BEACH}

The Barbados and Eastern Atlantic Combined High-altitude (BEACH) dropsonde dataset \citep{gloeckner2025} contains 1191 dropsondes deployed from the HALO aircraft during the ORCESTRA field campaign in August--September 2024.  Soundings were coordinated by the PERCUSION and MAESTRO subcampaigns and targeted the structure and variability of the Atlantic Inter-tropical Convergence Zone (ITCZ).  Circular flight patterns at approximately $\SI{14}{km}$ altitude (${\sim}\SI{260}{km}$ diameter) were flown over both the eastern and western tropical Atlantic, with flight tracks designed to coincide with EarthCARE satellite overpasses and to sample within and across the ITCZ.  Like JOANNE, the dataset includes a gridded product at $\SI{10}{m}$ spacing and derived mesoscale circulation diagnostics.  \citet{gloeckner2025} assign each profile an aggregate quality flag (GOOD, BAD, or UGLY) based on profile sparsity ($>$20\% of expected data present), profile extent above $\SI{8000}{m}$, near-surface coverage ($>$50 measurements in the lowest $\SI{1}{km}$), and physical plausibility of surface values.  Seventeen sondes were falsely configured by the manufacturer as minisondes rather than NRD41 (listed in supplement Table~E2 of \citealt{gloeckner2025}), and one sonde (0bd0e322, flight HALO-20240924a) had a factory-reset sensor module producing anomalous temperature and humidity.

Here, only profiles flagged as ``GOOD'' are used, and the 17 misconfigured and 1 factory-reset sondes listed above are excluded.

\subsection{OTREC}

The Organization of Tropical East Pacific Convection (OTREC) dropsonde dataset \citep{vomel2021} contains 648 dropsondes released from the NSF/NCAR Gulfstream-V during 22 research flights between August and October 2019.  The campaign investigated the distribution and day-to-day variability of deep atmospheric convection in the tropical eastern Pacific and Caribbean, with a focus on why higher rainfall rates occur over lower sea surface temperatures in this region.  Soundings were launched in grid patterns with a typical horizontal spacing of $1^\circ$ longitude and $1.2^\circ$ latitude, from a mean flight altitude of $\SI{13.3}{km}$ to the surface.  OTREC was the first field campaign to rely entirely on the NCAR NRD41 dropsonde, with a nominal vertical resolution between 6 and $\SI{12}{m}$.  All soundings in the distributed dataset have passed ASPEN quality control with manual post-processing corrections; erroneous data points are set to missing.  \citet{vomel2021} applied a per-sonde pressure offset correction (median $\SI{0.35}{hPa}$) and a relative humidity time-lag correction for the slow sensor response above ${\sim}\SI{11.5}{km}$.  One sounding (20190925\_154412) exhibits an unexplained warm bias of 1--$\SI{3.7}{K}$ throughout the profile.

Here, the corrected dataset is used as provided, with the exception of the warm-biased sounding noted above, which is excluded.

\subsection{ACTIVATE}

The Aerosol Cloud meTeorology Interactions oVer the western ATlantic Experiment (ACTIVATE) dropsonde dataset \citep{vomel2023activate} contains 801 dropsondes released from NASA Langley's King Air research aircraft during 165 flights between February 2020 and June 2022.  The campaign studied aerosol--cloud interactions over the western North Atlantic, off the coast of the eastern United States and around Bermuda, sampling all seasons across six deployment periods.  The King Air flew at approximately $\SI{9}{km}$ altitude while a coordinated HU-25 Falcon characterized trace gases, aerosol, and cloud properties in the lower atmosphere.  Dropsonde profiles provide temperature, pressure, relative humidity, and horizontal and vertical winds between the surface and flight level.  \citet{vomel2023activate} processed all soundings through ASPEN and applied corrected launch times for 17 sondes affected by a synchronization error between the onboard computer and GPS time.  Twenty sondes with unreconditioned humidity sensors may exhibit a dry bias.

Here, profiles marked as degraded are excluded.  The corrected launch times are used where applicable, and only temperature, pressure, and wind from the 20 sondes with unreconditioned humidity sensors is retained.

\subsection{Hurricane dropsonde dataset}

The NOAA hurricane GPS dropsonde dataset \citep{wang2015} consists of 13,681 atmospheric profiles collected during NOAA hurricane reconnaissance and surveillance flights between 1996 and 2012, covering 120 tropical cyclones.  Dropsondes were released from NOAA Gulfstream-IV and WP-3D aircraft in the inner and outer core regions of tropical cyclones, typically at $\SI{150}{}$--$\SI{200}{km}$ horizontal intervals.  The dataset uses the NCAR Airborne Vertical Atmospheric Profiling System (AVAPS), which underwent a major redesign in 2008 (AVAPS II) that expanded simultaneous sounding capacity to eight channels and improved GPS wind recovery rates.  \citet{wang2015} processed all profiles through ASPEN and performed visual inspection; the distributed dataset constitutes a single quality-controlled product.

Here, the archive is used as provided.

\subsection{DYNAMO}

The Dynamics of the Madden--Julian Oscillation (DYNAMO) upper-air sounding dataset \citep{ciesielski2014} contains nearly 26,000 soundings collected from an enhanced network of 72 rawinsonde sites and dropsonde data from 13 aircraft missions during October 2011 through March 2012.  The sounding network spanned the tropics from eastern Africa to the western Pacific, centered on the Indian Ocean where the MJO deep convection signal is first observed.  Six core sites in the central Indian Ocean formed two quadrilateral arrays with launch frequencies of up to eight soundings per day during the special observing period.  \citet{ciesielski2014} provide five processing levels (L0--L4) with increasingly refined quality control.  The Level~3 product incorporates solar radiation dry bias corrections for Vaisala RS92 daytime soundings, ship deck heating corrections, and inter-sonde-type humidity adjustments for the six different radiosonde models deployed across the network.

Here, only the dropsonde profiles are used, at the Level~3 correction tier.

\subsection{SHOUT}

The Sensing Hazards with Operational Unmanned Technology (SHOUT) project \citep{wick2020} deployed GPS dropsondes from NASA's Global Hawk unmanned aircraft during three field campaigns in 2015 and 2016.  The Global Hawk flew 15 missions sampling 6 tropical cyclones and 3 Pacific winter storms, with missions designed to target regions of high model forecast uncertainty identified through adjoint and ensemble sensitivity analyses.  The campaign evaluated whether high-altitude, long-endurance UAS observations could improve forecasts of high-impact weather events or help mitigate potential degradation from gaps in polar-orbiting satellite coverage.  All profiles were processed through ASPEN; the distributed dataset is a single quality-controlled product.

Here, the archive is used as provided.

\subsection{AR Recon}

Atmospheric River Reconnaissance (AR Recon) is a recurring campaign that has deployed dropsondes from NOAA G-IVSP and Air Force Reserve WC-130J aircraft during winter seasons (November--March) since 2016, targeting atmospheric rivers approaching the U.S.\ West Coast with the primary aim of improving forecasts of landfalling events.  \citet{cobb2022} describe the 2021 season, during which 1142 dropsondes were released during 45 flights.  The data used here span the 2018--2026 winter seasons.  Drop locations are guided by adjoint and ensemble sensitivity analyses to target regions where additional observations would most reduce forecast uncertainty.  All profiles were processed through ASPEN quality control.

Here, the archive is used as provided.

\section{Regridding algorithm}

The output grid is a one-dimensional partition of altitude into cells of uniform thickness $\Delta z$.  Cell edges are placed at
\begin{align}
    z_k = z_{\min} + k\,\Delta z, \qquad k = 0, 1, \ldots, N, \label{eq:edges}
\end{align}
where $z_{\min}$ is the lowest altitude of interest and $N = \lfloor(z_{\max} - z_{\min})/\Delta z\rfloor$ is the number of cells.  The $k$-th cell spans the half-open interval $[z_k,\, z_{k+1})$.  Cell-center altitudes, used as the coordinate for all output variables, are
\begin{align}
    \bar{z}_k = \tfrac{1}{2}(z_k + z_{k+1}) = z_{\min} + (k + \tfrac{1}{2})\,\Delta z. \label{eq:centers}
\end{align}
The default grid spacing is $\Delta z = \SI{10}{m}$, chosen to be finer than the effective vertical resolution of most modern radiosondes and dropsondes (of order $\SI{200}{m}$) so that no information is lost during regridding.

For each grid cell, the regridded value of a variable is the arithmetic mean of all measurements whose altitude falls within that cell.  If a cell contains no measurements, the value is recorded as missing.  Observations that are themselves missing or flagged are excluded.  This procedure is applied independently to each variable, so missing-value patterns may differ between variables.

\section{Effective vertical resolution} \label{sec:resolution}

The effective vertical resolution of a sonde profile is set by the combination of the sensor time constant, any post-processing smoothing, and the fall or ascent speed.  For the dropsonde datasets considered here, the dominant factor is the B-spline smoothing applied by the ASPEN processing software \citep{vomel2023}, not the sensor response times.

Dropsondes descend at approximately $\SI{11}{m\,s^{-1}}$ near the surface and $\SI{21}{m\,s^{-1}}$ near flight level (${\sim}\SI{13}{km}$), with the variation driven by the altitude dependence of air density.  Radiosondes ascend at approximately $\SI{5}{m\,s^{-1}}$.  The ASPEN smoothing wavelength varies by dataset: JOANNE applies a $\SI{10}{s}$ B-spline, while OTREC, ACTIVATE, and BEACH use $\SI{5}{s}$.  The resulting effective vertical resolutions are summarized in Table~\ref{tab:resolution}.

\begin{table}[h]
\centering
\caption{Effective vertical resolution by platform and ASPEN smoothing configuration.  The resolution range reflects the variation of fall speed with altitude.  Humidity resolution degrades at low temperatures due to the strongly temperature-dependent time constant of the thin-film capacitor sensor (up to ${\sim}\SI{60}{s}$ at flight level), but is smoothing-limited in the lower and middle troposphere.}
\label{tab:resolution}
\begin{tabular}{llll}
\toprule
Platform & Smoothing & Speed & Eff.\ resolution \\
\midrule
Dropsonde ($\SI{5}{s}$ ASPEN)  & $\SI{5}{s}$  & $11$--$\SI{21}{m\,s^{-1}}$ & $55$--$\SI{105}{m}$ \\
Dropsonde ($\SI{10}{s}$ ASPEN) & $\SI{10}{s}$ & $11$--$\SI{21}{m\,s^{-1}}$ & $110$--$\SI{210}{m}$ \\
Radiosonde                     & \textcolor{red}{FIGURE OUT RADIOSONDE SMOOTHING} & ${\sim}\SI{5}{m\,s^{-1}}$ & \textcolor{red}{TBD} \\
\bottomrule
\end{tabular}
\end{table}

\noindent The $\SI{10}{m}$ grid spacing is therefore finer than the effective resolution of all dropsonde products and comparable to the native resolution of radiosondes, ensuring that regridding does not discard information.  Structure functions computed from the regridded profiles should not be interpreted at vertical separations smaller than the effective resolution listed in Table~\ref{tab:resolution}.

\section{Variables} \label{sec:variables}

\subsection{Directly regridded variables}

The following variables are bin-averaged onto the uniform grid:

\begin{center}
\begin{tabular}{lll}
\toprule
Symbol & Description & Units \\
\midrule
$u$    & Zonal wind component         & \si{m\per s} \\
$v$    & Meridional wind component     & \si{m\per s} \\
$p$    & Pressure                      & \si{Pa} \\
$T$    & Temperature                   & \si{K} \\
$\mathrm{RH}$  & Relative humidity     & \% \\
\bottomrule
\end{tabular}
\end{center}

\noindent Vertical wind is not retained.  Dropsonde vertical wind estimates are derived from the GPS fall rate after subtracting a modeled still-air fall speed, and the resulting uncertainty is too large for the analyses intended here.

When a dataset provides the water vapor mixing ratio $q$ directly rather than relative humidity, $q$ is bin-averaged in place of $\mathrm{RH}$ and no subsequent diagnosis is needed.

\subsection{Diagnosed variables} \label{sec:diagnosed}

Five additional variables are diagnosed from the regridded fields.  Diagnosis is performed \emph{after} bin averaging so that the thermodynamic relations are evaluated on the gridded values rather than on individual high-frequency measurements.  This ordering is consistent with the interpretation that each grid cell represents a local mean state.

\subsubsection{Water vapor mixing ratio}

When relative humidity is the reported moisture variable, the mixing ratio is computed from the regridded $\mathrm{RH}$, $T$, and $p$ fields.  The saturation vapor pressure over liquid water $e_s$ is obtained from the August--Roche--Magnus formula,
\begin{align}
    e_s(T) = 610.94 \exp\!\left(\frac{17.625\, T_c}{T_c + 243.04}\right), \label{eq:es}
\end{align}
where $T_c = T - \SI{273.15}{K}$ is the temperature in degrees Celsius and $e_s$ has units of Pascals.  The vapor pressure is $e = (\mathrm{RH}/100)\, e_s$, and the mixing ratio follows as
\begin{align}
    q = \frac{0.622\, e}{p - e}. \label{eq:q}
\end{align}
The constant $0.622$ is the ratio of the molecular mass of water to the mean molecular mass of dry air.

\subsubsection{Potential temperature}

Potential temperature is computed from Poisson's equation,
\begin{align}
    \theta = T \left(\frac{p_0}{p}\right)^{\kappa}, \label{eq:theta}
\end{align}
where $p_0 = \SI{100000}{Pa}$ is the reference pressure and $\kappa = R_d / c_p \approx 0.2854$ is the Poisson constant, with $R_d = \SI{287.04}{J\,kg^{-1}\,K^{-1}}$ the specific gas constant for dry air and $c_p = \SI{1005.7}{J\,kg^{-1}\,K^{-1}}$ the isobaric specific heat.

\subsubsection{Moist static energy}

Moist static energy combines sensible heat, gravitational potential energy, and latent heat into a single conserved quantity for moist adiabatic processes:
\begin{align}
    h = c_p T + g\bar{z}_k + L_v q, \label{eq:MSE}
\end{align}
where $g = \SI{9.807}{m\,s^{-2}}$ is gravitational acceleration, $L_v = \SI{2.501e6}{J\,kg^{-1}}$ is the latent heat of vaporization at $\SI{0}{\celsius}$, and $\bar{z}_k$ is the cell-center altitude from Eq.~\ref{eq:centers}.

\subsubsection{Dry static energy}

Dry static energy is conserved for dry adiabatic processes:
\begin{align}
    s = c_p T + g\bar{z}_k. \label{eq:DSE}
\end{align}

\subsubsection{Equivalent potential temperature}

Equivalent potential temperature is conserved for saturated adiabatic processes.  It is approximated following \citet{bolton1980}:
\begin{align}
    \theta_e = \theta \exp\!\left(\frac{L_v\, q}{c_p\, T_L}\right), \label{eq:theta_e}
\end{align}
where $T_L$ is the temperature at the lifting condensation level, estimated as
\begin{align}
    T_L = \frac{1}{\displaystyle\frac{1}{T - 55} - \frac{\ln(\mathrm{RH}/100)}{2840}} + 55. \label{eq:TL}
\end{align}

\section{Output format} \label{sec:output}

The regridded data are stored as one NetCDF-4 file per source dataset.  Each file has two dimensions: \texttt{sonde}, indexing individual profiles, and \texttt{altitude}, indexing the uniform grid cells.

\subsection{Coordinates}

The \texttt{altitude} coordinate contains the cell-center altitudes $\bar{z}_k$ from Eq.~\ref{eq:centers}, in meters above mean sea level.

\subsection{Per-sonde metadata}

Each profile carries the following scalar metadata along the \texttt{sonde} dimension:

\begin{center}
\begin{tabular}{lll}
\toprule
Variable & Type & Description \\
\midrule
\texttt{launch\_time}  & datetime & Date and time of profile start (launch or drop) \\
\texttt{launch\_lat}   & float    & Latitude at profile start [\si{\degree}N] \\
\texttt{launch\_lon}   & float    & Longitude at profile start [\si{\degree}E] \\
\texttt{sonde\_id}     & string   & Provider's sonde or profile identifier \\
\texttt{station\_id}   & string   & Station identifier (radiosondes) or flight ID (dropsondes) \\
\bottomrule
\end{tabular}
\end{center}

\noindent For dropsonde datasets, \texttt{launch\_lat} and \texttt{launch\_lon} refer to the aircraft position at the time of release.  For radiosondes, they refer to the launch site.

\subsection{Data variables}

All regridded and diagnosed variables are stored with shape (\texttt{sonde}, \texttt{altitude}).  Missing values (grid cells with no measurements) are stored as NaN.

\begin{center}
\begin{tabular}{lll}
\toprule
Variable & Description & Units \\
\midrule
\texttt{u}          & Zonal wind component             & \si{m\per s} \\
\texttt{v}          & Meridional wind component         & \si{m\per s} \\
\texttt{p}          & Pressure                          & \si{Pa} \\
\texttt{T}          & Temperature                       & \si{K} \\
\texttt{RH}         & Relative humidity                 & \% \\
\texttt{q}          & Water vapor mixing ratio          & \si{kg\per kg} \\
\texttt{theta}      & Potential temperature              & \si{K} \\
\texttt{theta\_e}   & Equivalent potential temperature   & \si{K} \\
\texttt{MSE}        & Moist static energy               & \si{J\per kg} \\
\texttt{DSE}        & Dry static energy                 & \si{J\per kg} \\
\bottomrule
\end{tabular}
\end{center}

\noindent Variables that depend on relative humidity (\texttt{q}, \texttt{theta\_e}, \texttt{MSE}) are entirely NaN for profiles where RH is unavailable---for example, the 20 ACTIVATE sondes with unreconditioned humidity sensors, or IGRA stations that do not report humidity.  \texttt{theta} and \texttt{DSE} require only temperature and pressure and are present for all profiles.

\section{Validation} \label{sec:validation}

Two of the dropsonde datasets---JOANNE \citep{george2021} and BEACH \citep{gloeckner2025}---provide both individual sounding profiles and a uniformly gridded product at $\SI{10}{m}$ vertical spacing produced by the data providers.  These gridded products serve as natural validation targets: applying the bin-averaging algorithm described here to the individual profiles and comparing the result against the official gridded product tests whether the two procedures yield consistent output.

Agreement is not expected to be exact.  The provider gridded products use interpolation rather than bin averaging, apply additional quality control beyond what is present in the individual profiles, and in the case of BEACH use slightly different filtering criteria.  The purpose of the comparison is to verify broad consistency with an independent processing chain.

For each variable, the comparison is performed per sounding at all grid cells where both products report non-missing values.  For each sounding, the RMSD and mean bias (bin-averaged minus provider-gridded) are computed over the matched cells.  The summary statistics reported are the \emph{median} RMSD and \emph{mean} bias across all matched soundings.  These are then compared against thresholds based on the measurement uncertainty of the Vaisala RD41 dropsonde [\textcolor{red}{CITATION NEEDED}]:

\begin{center}
\begin{tabular}{llll}
\toprule
Variable & Instrument repeatability & RMSD threshold \\
\midrule
$T$               & \SI{0.1}{K}      & \SI{0.5}{K} \\
$u$, $v$          & \SI{0.15}{m\per s} & \SI{0.5}{m\per s} \\
$\mathrm{RH}$     & 2\%              & 5\% \\
$p$               & \SI{0.4}{hPa}    & \SI{1}{hPa} \\
\bottomrule
\end{tabular}
\end{center}

\noindent The thresholds are set at several times the instrument repeatability to accommodate the genuine methodological difference between interpolation and bin averaging, particularly in cells with sparse measurements where the two approaches can diverge.  If the RMSD for each variable falls within these bounds and the mean bias is small relative to the RMSD, the regridding algorithm is considered consistent with the provider processing for the purposes of this study.

\bibliographystyle{apalike}
\bibliography{references}

\end{document}
